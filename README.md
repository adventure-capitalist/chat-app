This is a simple example of using react to render the same component repeatedly but differently.

An API is used to generate a random user, and their status (online or offline) is also determined randomly.
